# include <stdio.h>
int main() {
   int side ;
   printf("side:");    
   scanf("%d",&side);    // if float value then %f instead of 
   printf("area of square :%d",side*side);
   return 0 ;

}