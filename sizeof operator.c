
#include<stdio.h>
int main(){
   int a ;
   char b;
   float c; 
    printf(" size of a is:%lu \n",sizeof(a));
    printf(" size of b is:%lu \n",sizeof(b));
    printf(" size of c is:%lu \n",sizeof(c));
    return 0;
}


// %lu is a format specifier used with the printf and scanf functions for dealing with unsigned long integer data types. It is used to format the output or input of unsigned long integers.